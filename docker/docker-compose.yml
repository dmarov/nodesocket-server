version: "3.9"
services:
  socket-server:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
      target: image-prod
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - CONFIG_PATH=configs/env-prod.yml
